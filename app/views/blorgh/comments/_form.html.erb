<%=form_for [@article, @article.comments.build] do |f| %>
		<%= render 'blorgh/shared/error_messages', object: f.object %>
		<%=f.label :comment, class: "form-control"  %>
		<%= f.text_field :text %>
		<br>
		
		<%=f.submit :comment, class:'btn'  %>
<% end %>
